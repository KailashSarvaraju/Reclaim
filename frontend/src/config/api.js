// Centralized API configuration
// Defaults to localhost:8000 for development; can be overridden via environment variable
const API_BASE = "https://reclaim-vo5u.onrender.com";
export default API_BASE;